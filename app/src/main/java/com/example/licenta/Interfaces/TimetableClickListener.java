package com.example.licenta.Interfaces;

import com.example.licenta.Models.TimetableRowModel;

public interface TimetableClickListener {
    void OnClick(TimetableRowModel timetableRowModel, int position);
}
